math_sym, value_x, value_y = input('Введите последовательность в формате: математический символ, "пробел", x, "пробел", y ').split()
#Создаем список из введенных параметров
x = [math_sym,value_x,value_y]
y = []

for item in x:
  math_sym = x[0]
  value_x = int(x[1])
  value_y = int(x[2])
  y = [math_sym,value_x,value_y]

def calculate(list_var):
  try:
    if list_var[0] == "*":
      num1 = list_var[1] * list_var[2]
      return num1
    elif list_var[0] == "/":
      num1 = list_var[1] / list_var[2]
      return num1
    elif list_var[0] == "+":
      num1 = list_var[1] + list_var[2]
      return num1
    else:
      num1 = list_var[1] - list_var[2]
      return num1

  except ZeroDivisionError: 
    print ("division by zero")

print(calculate(y))

